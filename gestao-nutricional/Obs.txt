Acesse o sistema:

Tela inicial: http://localhost/gestao-nutricional/Tela1.html

Painel: http://localhost/gestao-nutricional/Tela2.php

MYSQL necessário: 

CREATE TABLE dependentes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    cliente_id INT NOT NULL,
    nome VARCHAR(100) NOT NULL,
    data_nascimento DATE NOT NULL,
    parentesco VARCHAR(50) NOT NULL
);

CREATE TABLE consultas (
    id INT AUTO_INCREMENT PRIMARY KEY,
    dependente_id INT NOT NULL,
    data_consulta DATE NOT NULL,
    observacoes TEXT,
    FOREIGN KEY (dependente_id) REFERENCES dependentes(id)
);

CREATE TABLE historico_imc (
    id INT AUTO_INCREMENT PRIMARY KEY,
    dependente_id INT NOT NULL,
    valor_imc DECIMAL(5,2) NOT NULL,
    classificacao VARCHAR(50) NOT NULL,
    data_calculo TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (dependente_id) REFERENCES dependentes(id)
);